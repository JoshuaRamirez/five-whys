---
max_turns: 40
timeout_seconds: 1800
allowed_tools: [Skill, Agent, Bash, Read, Write, Edit, Glob, Grep]
---

/five-ws --ask what,how --depth 1 --yes Our nightly database backup job silently skips some runs, and nobody notices until a restore is needed.
